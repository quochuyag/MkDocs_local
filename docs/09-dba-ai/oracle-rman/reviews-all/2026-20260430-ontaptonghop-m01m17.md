---
title: 📚 Ôn Tập Tổng Hợp — Oracle RMAN 17 Modules
course: 09-dba-ai
source: dba_ai/oracle_rman/reviews_all/20260430_OntapTongHop_M01M17.md
---

# 📚 Ôn Tập Tổng Hợp — Oracle RMAN 17 Modules

**Phạm vi**: Module 01 → Module 17 (toàn khóa)
**Ngày tạo**: 2026-04-30
**Lab target**: Oracle 12c · SID=ORADB · 192.168.1.8
**Mục tiêu**: Củng cố toàn bộ kiến thức trước khi thi / áp dụng production

---

## Mục lục

- [Phần 1 – Bản đồ kiến thức (Mind Map)](#phần-1--bản-đồ-kiến-thức)
- [Phần 2 – Flashcards: Khái niệm cốt lõi](#phần-2--flashcards-khái-niệm-cốt-lõi)
- [Phần 3 – 30 câu hỏi trắc nghiệm Mock Exam](#phần-3--30-câu-hỏi-trắc-nghiệm-mock-exam)
- [Phần 4 – Fire Drills tổng hợp (5 kịch bản)](#phần-4--fire-drills-tổng-hợp)
- [Phần 5 – Bảng lệnh tra cứu nhanh](#phần-5--bảng-lệnh-tra-cứu-nhanh)
- [Phần 6 – Sơ đồ quyết định Recovery](#phần-6--sơ-đồ-quyết-định-recovery)
- [Đáp án Mock Exam](#đáp-án-mock-exam)

---

## Phần 1 – Bản đồ kiến thức

```
Oracle RMAN — Toàn bộ 17 Modules
│
├── NHÓM 1: Nền tảng (M01–M03)
│   ├── M01: Lab Setup – VirtualBox, 2 VM (srv1 + winsrv2)
│   ├── M02: Backup Theory – 12 loại failure, RPO/RTO, FRA, ARCHIVELOG
│   └── M03: RMAN Basics – Kết nối, SYSBACKUP, Persistent Settings
│
├── NHÓM 2: Backup Techniques (M04–M06)
│   ├── M04: Full Backups – Backup Set vs Image Copy, Hot vs Cold
│   ├── M05: Incremental – Level 0/1 Diff/Cumul, BCT, Updated Image Copy
│   └── M06: Settings – Retention Policy, Channel, Format, Deletion Policy
│
├── NHÓM 3: Advanced Backup (M07–M11)
│   ├── M07: Reporting – LIST/REPORT/CROSSCHECK, V$SESSION_LONGOPS
│   ├── M08: Improvement – Compress, Multisection, Duplex, KEEP FOREVER
│   ├── M09: Catalog – rcowner, CREATE CATALOG, Stored Script
│   ├── M10: Encryption – Transparent / Password / Dual mode
│   └── M11: Best Practices – D2D2T, RTO/RPO matrix
│
├── NHÓM 4: Recovery (M12–M13)
│   ├── M12: Recovery – Complete, PITR, Tablespace, Table, Control File
│   └── M13: DRA & Block Corruption – LIST/ADVISE/REPAIR FAILURE, BMR
│
└── NHÓM 5: Advanced Topics (M14–M17)
    ├── M14: Cross-Platform – Endian, CONVERT, Foreign Tablespace
    ├── M15: Duplication – Active vs Backup-based, DBID tự đổi
    ├── M16: Tuning & Troubleshoot – I/O slaves, DURATION, Debug trace
    └── M17: CDB/PDB, RAC, Cloud – Multitenant backup, Snapshot CF
```

---

## Phần 2 – Flashcards: Khái niệm cốt lõi

### 🟦 Nhóm 1: Nền tảng

| Câu hỏi | Trả lời |
|---------|---------|
| FRA warning/critical threshold? | Warning **85%**, Critical **97%** → DB có thể HANG |
| RPO vs RTO khác nhau thế nào? | **RPO** = Mất tối đa bao nhiêu data; **RTO** = Mất bao lâu để khôi phục |
| Khi nào BẮT BUỘC dùng ARCHIVELOG mode? | Production OLTP và mọi khi cần hot backup (DB đang OPEN) |
| Control file nên có bao nhiêu bản? | **≥ 2 bản** (multiplexing) trên đĩa vật lý khác nhau |
| CONTROL_FILE_RECORD_KEEP_TIME mặc định? | **7 ngày** — tăng lên ≥ 30 ngày cho production |
| SYSBACKUP khác SYSDBA thế nào? | SYSBACKUP chỉ backup/recovery, không đọc được data user |

### 🟩 Nhóm 2: Backup Techniques

| Câu hỏi | Trả lời |
|---------|---------|
| Backup Set vs Image Copy — cái nào ra tape được? | **Backup Set** ra được tape; Image Copy chỉ ra disk |
| Image Copy có lợi thế gì? | Dùng `SWITCH` ngay lập tức, không cần RESTORE, **RTO siêu nhanh** |
| Incremental Level 1 Differential backup từ đâu? | Từ Level 0 hoặc **Level 1 gần nhất** |
| Incremental Level 1 Cumulative backup từ đâu? | Luôn luôn từ **Level 0 gần nhất** |
| Block Change Tracking (BCT) làm gì? | Track các block đã thay đổi → incremental backup nhanh hơn đáng kể |
| Retention Policy: chọn Recovery Window hay Redundancy? | Chỉ chọn **1 trong 2** — không dùng đồng thời |
| `BACKUP NOT BACKED UP SINCE TIME 'SYSDATE-1'` dùng để làm gì? | Chỉ backup file chưa được backup trong 1 ngày — tránh backup lại lần 2 |

### 🟨 Nhóm 3: Advanced Backup

| Câu hỏi | Trả lời |
|---------|---------|
| EXPIRED vs OBSOLETE khác nhau thế nào? | **EXPIRED** = file vật lý mất; **OBSOLETE** = quá hạn retention |
| Lệnh nào phát hiện EXPIRED? | `CROSSCHECK BACKUP` |
| Lệnh nào xóa OBSOLETE? | `DELETE OBSOLETE` |
| Recovery Catalog cần DB riêng không? | **Có** — không đặt catalog trên cùng target DB |
| Catalog lưu thêm gì so với Control File? | Stored Scripts, lịch sử dài hạn, quản lý nhiều DB |
| Encryption chỉ dùng được với loại backup nào? | Chỉ **Backup Set**, không dùng với Image Copy |
| KEEP FOREVER nghĩa là gì? | Backup không bao giờ bị OBSOLETE — cần Recovery Catalog để quản lý lâu dài |
| D2D2T là gì? | Disk → Disk → Tape: FRA ngắn hạn + Tape dài hạn off-site |

### 🟥 Nhóm 4: Recovery

| Câu hỏi | Trả lời |
|---------|---------|
| RESTORE vs RECOVER khác nhau? | **RESTORE** = lấy file ra khỏi backup; **RECOVER** = apply archive logs vào file |
| Complete Recovery mở DB bằng lệnh nào? | `ALTER DATABASE OPEN` (thông thường) |
| Incomplete Recovery (PITR) mở DB bằng lệnh nào? | `ALTER DATABASE OPEN RESETLOGS` |
| TSPITR là gì? | Tablespace Point-in-Time Recovery — khôi phục 1 tablespace về quá khứ |
| TSPITR cần gì đặc biệt? | **Auxiliary destination** — thư mục tạm để dựng Auxiliary instance |
| Mất Control File → làm gì đầu tiên? | `SET DBID xxxxx` rồi `STARTUP NOMOUNT` rồi `RESTORE CONTROLFILE FROM AUTOBACKUP` |
| Redo Log group INACTIVE bị hỏng → lệnh gì? | `ALTER DATABASE CLEAR LOGFILE GROUP n` |
| DRA không hỗ trợ môi trường nào? | **RAC** (Real Application Clusters) |
| Block Media Recovery (BMR) cần gì? | File backup còn hợp lệ + archive logs chứa changes của block đó |

### 🟪 Nhóm 5: Advanced Topics

| Câu hỏi | Trả lời |
|---------|---------|
| Linux vs AIX — endian format khác nhau không? | Có: Linux/Windows = **Little Endian**; AIX/Solaris = **Big Endian** |
| Khác endian thì phải làm gì khi transport? | Chạy `CONVERT TABLESPACE` hoặc `CONVERT DATAFILE` |
| Active Duplication cần gì? | Network giữa source và destination, không cần file backup trên disk |
| Duplication có thay đổi DBID tự động không? | **Có** — RMAN tự đổi DBID cho bản clone |
| Nguy hiểm nhất khi Duplication là gì? | Quên `DB_FILE_NAME_CONVERT` → ghi đè lên Production |
| CDB Root backup `BACKUP DATABASE` bao gồm gì? | ROOT + tất cả PDB + archivelogs |
| RAC: Snapshot Control File phải để ở đâu? | **Shared Storage** (ASM) — tất cả nodes đều truy cập được |
| Cloud backup bắt buộc phải làm gì? | **Mã hóa** trước khi push lên OCI |

---

## Phần 3 – 30 câu hỏi trắc nghiệm Mock Exam

*Ghi lại đáp án của bạn rồi đối chiếu với [Đáp án](#đáp-án-mock-exam)*

---

**1.** FRA sẽ gây ra lỗi `ORA-19809` khi đạt ngưỡng nào?

```
A) 70%
B) 85%
C) 97%
D) 100%
```

---

**2.** Bạn muốn khôi phục database đến đúng **trước khi** có lệnh `DROP TABLE` xảy ra lúc 14:00. Bạn cần dùng loại recovery nào?

```
A) Complete Recovery
B) Incomplete Recovery (PITR)
C) Block Media Recovery
D) Tablespace Recovery
```

---

**3.** Câu lệnh nào dùng để kiểm tra xem file backup vật lý có còn tồn tại không?

```
A) LIST BACKUP SUMMARY
B) REPORT NEED BACKUP
C) CROSSCHECK BACKUP
D) VALIDATE DATABASE
```

---

**4.** `BACKUP INCREMENTAL LEVEL 1 DATABASE` — đây là loại gì nếu không có từ khóa `CUMULATIVE`?

```
A) Cumulative incremental
B) Differential incremental
C) Full backup
D) Image Copy
```

---

**5.** Lệnh nào dùng để switch ngay lập tức sang dùng Image Copy mà **không cần** restore?

```
A) RESTORE DATAFILE ... FROM TAG
B) RECOVER DATAFILE ... FROM COPY
C) SWITCH DATAFILE N TO COPY
D) CATALOG DATAFILECOPY
```

---

**6.** Block Change Tracking file được bật bằng lệnh nào?

```
A) RMAN> CONFIGURE BLOCK CHANGE TRACKING ON
B) SQL> ALTER DATABASE ENABLE BLOCK CHANGE TRACKING
C) SQL> ALTER SYSTEM SET BLOCK_CHANGE_TRACKING = TRUE
D) RMAN> SET BLOCK TRACKING ON
```

---

**7.** Bạn cần backup chạy trong vòng tối đa 4 giờ, nếu hết giờ thì dừng nhưng backup hiện tại vẫn hợp lệ. Lệnh nào phù hợp?

```
A) BACKUP DATABASE MAXTIME 4:00
B) BACKUP DATABASE DURATION 04:00 PARTIAL MINIMIZE TIME
C) BACKUP DATABASE TIMEOUT 240
D) BACKUP DATABASE SECTION SIZE 4G
```

---

**8.** Recovery Catalog được tạo trong schema nào?

```
A) SYS
B) SYSTEM
C) RCOWNER (hoặc tên DBA tự tạo)
D) RMAN
```

---

**9.** Câu nào ĐÚNG về encryption trong RMAN?

```
A) Encryption dùng được với cả Backup Set và Image Copy
B) Transparent mode cần nhập password thủ công mỗi lần backup
C) Encryption chỉ áp dụng cho Backup Set
D) Password mode dùng TDE Keystore tự động
```

---

**10.** Lệnh DRA theo đúng thứ tự là?

```
A) ADVISE FAILURE → LIST FAILURE → REPAIR FAILURE
B) LIST FAILURE → REPAIR FAILURE → ADVISE FAILURE
C) LIST FAILURE → ADVISE FAILURE → REPAIR FAILURE
D) DIAGNOSE FAILURE → LIST FAILURE → REPAIR FAILURE
```

---

**11.** Bạn muốn backup tablespace `FIN` sang platform Linux 64-bit từ AIX (Big Endian). Bước đầu tiên là?

```
A) RESTORE FOREIGN TABLESPACE fin
B) CONVERT TABLESPACE fin TO PLATFORM 'Linux IA (64-bit)'
C) BACKUP TABLESPACE fin FOR TRANSPORT
D) ALTER TABLESPACE fin READ ONLY
```

---

**12.** `CONFIGURE RETENTION POLICY TO REDUNDANCY 3` có nghĩa là?

```
A) Giữ backup trong 3 ngày
B) Giữ 3 bản backup gần nhất, bản cũ hơn → OBSOLETE
C) Backup chạy 3 lần/tuần
D) Backup song song 3 channels
```

---

**13.** Khi chạy `BACKUP DATABASE PLUS ARCHIVELOG`, archive logs sẽ được backup vào?

```
A) Sau khi backup datafiles hoàn tất
B) Trước và sau khi backup datafiles
C) Chỉ sau khi backup datafiles
D) Không backup archive logs trong lệnh này
```

---

**14.** `CONFIGURE ARCHIVELOG DELETION POLICY BACKED UP 1 TIMES TO DEVICE TYPE DISK` nghĩa là?

```
A) Xóa archive log ngay sau khi tạo
B) Chỉ xóa archive log khi đã backup ≥ 1 lần lên DISK
C) Xóa archive log sau 1 ngày
D) Backup archive log 1 lần/ngày
```

---

**15.** Mất toàn bộ online redo log group **INACTIVE** — giải pháp là?

```
A) ALTER DATABASE DROP LOGFILE GROUP n; rồi tạo lại
B) ALTER DATABASE CLEAR LOGFILE GROUP n
C) RMAN> RECOVER LOGFILE GROUP n
D) RESTORE REDO từ backup
```

---

**16.** Câu lệnh nào tạo ra bản backup **không bao giờ bị OBSOLETE**?

```
A) BACKUP DATABASE TAG 'PERM'
B) BACKUP DATABASE KEEP FOREVER RESTORE POINT rp1
C) BACKUP DATABASE REDUNDANCY FOREVER
D) CONFIGURE RETENTION POLICY TO RECOVERY WINDOW OF 999 DAYS
```

---

**17.** Trong Table Recovery với RMAN, `REMAP TABLE HR.EMP:TEST.RECOVERED_EMP` có nghĩa là?

```
A) Đổi tên table từ HR.EMP thành RECOVERED_EMP
B) Recover table về schema TEST với tên RECOVERED_EMP thay vì ghi đè HR.EMP
C) Copy structure của HR.EMP vào TEST
D) Tạo synonym RECOVERED_EMP trỏ về HR.EMP
```

---

**18.** `BACKUP COPIES 2 DATABASE FORMAT '/disk1/%U', '/disk2/%U'` tạo ra?

```
A) 1 bản backup chia làm 2 phần
B) 2 bản backup giống hệt nhau ở 2 vị trí khác nhau
C) Backup chạy 2 lần vào giờ khác nhau
D) 2 backup với 2 compression level khác nhau
```

---

**19.** Câu nào ĐÚNG về Active Database Duplication?

```
A) Cần copy file backup ra đĩa của destination trước
B) Kết nối trực tiếp qua Oracle Net, không cần file backup
C) Chỉ dùng được trong cùng 1 server
D) DBID của bản clone giữ nguyên như source
```

---

**20.** V$SESSION_LONGOPS dùng để?

```
A) Xem lịch sử backup
B) Monitor tiến độ RMAN backup/recovery realtime
C) Xem các session đang chờ lock
D) Kiểm tra FRA usage
```

---

**21.** `CONFIGURE DEVICE TYPE DISK PARALLELISM 2` nghĩa là?

```
A) Backup chạy 2 lần
B) Tự động allocate 2 channel khi không dùng RUN{}
C) Tốc độ disk tăng gấp đôi
D) Backup chia làm 2 pieces
```

---

**22.** Sau PITR, lý do phải chạy `ALTER DATABASE OPEN RESETLOGS` là?

```
A) Để tạo mới Redo Log
B) Để đặt lại SCN sequence, tránh conflict với archive logs cũ
C) Để xóa hết backup cũ
D) Bắt buộc sau mọi loại recovery
```

---

**23.** `VALIDATE DATABASE` khác `VALIDATE CHECK LOGICAL DATABASE` thế nào?

```
A) Không khác gì cả
B) CHECK LOGICAL bổ sung kiểm tra logical corruption (inter-block)
C) VALIDATE DATABASE chỉ kiểm tra header block
D) CHECK LOGICAL chạy nhanh hơn
```

---

**24.** Khi backup CDB từ Root connection, lệnh `BACKUP DATABASE` sẽ backup?

```
A) Chỉ CDB$ROOT
B) CDB$ROOT + PDB đang OPEN
C) Tất cả PDB nhưng không có ROOT
D) ROOT + tất cả PDB + archive logs
```

---

**25.** `CONFIGURE CONTROLFILE AUTOBACKUP ON` — control file tự backup khi nào?

```
A) Mỗi giờ
B) Sau mỗi lần chạy BACKUP hoặc CHANGE DATABASE command
C) Chỉ khi chạy BACKUP DATABASE
D) Sau mỗi lần CONFIGURE
```

---

**26.** Khác biệt chính giữa TSPITR và Table Recovery là?

```
A) TSPITR nhanh hơn vì không cần Auxiliary
B) Table Recovery phục hồi 1 table cụ thể; TSPITR phục hồi nguyên tablespace
C) TSPITR chỉ dùng khi mất toàn bộ datafile
D) Table Recovery cần RESETLOGS
```

---

**27.** Câu lệnh `CATALOG START WITH '/media/backup_dir/'` dùng để?

```
A) Tạo Recovery Catalog mới
B) Đăng ký file backup bên ngoài (không trong FRA) vào catalog
C) Tạo backup mới vào thư mục đó
D) Mount thư mục NFS vào RMAN
```

---

**28.** Trong RAC, Snapshot Control File phải nằm ở đâu?

```
A) Local disk của node 1
B) /tmp trên mỗi node
C) Shared Storage (ASM) mà tất cả nodes đều access được
D) Control File thông thường không cần Snapshot trong RAC
```

---

**29.** `MULTISECTION` backup (SECTION SIZE) đặc biệt hữu ích khi?

```
A) Database nhỏ < 10GB
B) Có datafile rất lớn và muốn backup song song nhiều channel
C) Kết nối mạng chậm đến Catalog
D) Cần mã hóa backup
```

---

**30.** Sau khi chạy `CROSSCHECK BACKUP`, một backup piece hiện trạng thái `EXPIRED`. Bước tiếp theo hợp lý nhất là?

```
A) RESTORE lại piece đó từ tape
B) DELETE EXPIRED BACKUP để loại bỏ record không còn dùng được
C) REPORT OBSOLETE để xác nhận lại
D) RESYNC CATALOG
```

---

## Phần 4 – Fire Drills tổng hợp

---

### Fire Drill 1 — Control File bị mất hoàn toàn

**Bối cảnh**: Database crash, toàn bộ control file mất. DBID bạn nhớ là **1234567890**. Có Autobackup Control File đang bật. FRA = `/u02/fra`.

**Nhiệm vụ**: Viết đầy đủ các bước để khôi phục và mở database.

<details>
<summary>▶ Xem đáp án</summary>

```rman
-- Bước 1: Kết nối khi DB chưa MOUNT được
rman target /

-- Bước 2: Set DBID (vì không có control file, RMAN không tự biết)
SET DBID 1234567890;

-- Bước 3: Start NOMOUNT (chỉ cần SPFILE/PFILE)
STARTUP NOMOUNT;

-- Bước 4: Restore control file từ autobackup
RESTORE CONTROLFILE FROM AUTOBACKUP;

-- Bước 5: Mount database với control file vừa restore
ALTER DATABASE MOUNT;

-- Bước 6: Restore và recover toàn bộ database
RESTORE DATABASE;
RECOVER DATABASE;

-- Bước 7: Mở database (phải RESETLOGS sau khi recover từ control file)
ALTER DATABASE OPEN RESETLOGS;

-- Bước 8: Backup ngay sau khi khôi phục
BACKUP DATABASE PLUS ARCHIVELOG;
```

**Lưu ý**: Nếu không nhớ DBID, tìm trong alert log hoặc `V$DATABASE.DBID`.

</details>

---

### Fire Drill 2 — DROP TABLE nhầm lúc 09:00, recover về 08:55

**Bối cảnh**: Dev vô tình chạy `DROP TABLE HR.EMPLOYEES` lúc 09:00 trên production. Database ở ARCHIVELOG mode, backup đầy đủ. Yêu cầu recover đúng table về thời điểm 08:55.

**Nhiệm vụ**: Dùng Table Recovery (M12). Auxiliary destination = `/tmp/tableaux`.

<details>
<summary>▶ Xem đáp án</summary>

```rman
-- Cách 1: Table Recovery (Oracle 12c+, đơn giản nhất)
rman target /

RECOVER TABLE HR.EMPLOYEES
  UNTIL TIME "TO_DATE('2026-04-30 08:55:00','YYYY-MM-DD HH24:MI:SS')"
  AUXILIARY DESTINATION '/tmp/tableaux'
  DATAPUMP DESTINATION '/tmp/datapump'
  DUMP FILE 'recovered_emp.dmp'
  NOTABLEIMPORT;

-- Sau đó import thủ công:
-- impdp hr/hr DUMPFILE=recovered_emp.dmp TABLES=EMPLOYEES REMAP_TABLE=EMPLOYEES:EMPLOYEES_RECOVERED

-- Cách 2: REMAP ngay khi recover (nếu muốn giữ table cũ và tạo table mới)
RECOVER TABLE HR.EMPLOYEES
  UNTIL TIME "TO_DATE('2026-04-30 08:55:00','YYYY-MM-DD HH24:MI:SS')"
  AUXILIARY DESTINATION '/tmp/tableaux'
  REMAP TABLE HR.EMPLOYEES:HR.EMPLOYEES_RECOVERED;
```

**Không dùng PITR toàn database** vì sẽ mất mọi data từ 08:55 đến 09:00 của các table khác.

</details>

---

### Fire Drill 3 — FRA đầy 100%, database treo

**Bối cảnh**: 03:00 sáng. Alert: `ORA-19809`. DB vẫn OPEN nhưng mọi write transaction đang treo. FRA = 10GB, RETENTION = Recovery Window 7 Days. Không có Tape.

**Nhiệm vụ**: Xử lý khẩn cấp trong 5 phút, sau đó phòng ngừa tái phát.

<details>
<summary>▶ Xem đáp án</summary>

```sql
-- Bước 1: Chẩn đoán nhanh qua SQL*Plus
SELECT FILE_TYPE,
       ROUND(PERCENT_SPACE_USED,1) AS PCT_USED,
       ROUND(PERCENT_SPACE_RECLAIMABLE,1) AS PCT_RECLAIM,
       NUMBER_OF_FILES
FROM V$RECOVERY_AREA_USAGE
ORDER BY PERCENT_SPACE_USED DESC;
```

```rman
-- Bước 2: Crosscheck rồi dọn ngay
rman target /

CROSSCHECK BACKUP;
CROSSCHECK ARCHIVELOG ALL;

-- Xóa backup hết hạn
DELETE NOPROMPT OBSOLETE;

-- Nếu vẫn chưa đủ: xóa archive log đã được backup
DELETE NOPROMPT ARCHIVELOG ALL BACKED UP 1 TIMES TO DEVICE TYPE DISK;
```

```sql
-- Bước 3: Tăng FRA size tạm thời (không restart DB)
ALTER SYSTEM SET DB_RECOVERY_FILE_DEST_SIZE = 20G SCOPE=BOTH;
```

```rman
-- Phòng ngừa dài hạn: setup retention + auto delete
CONFIGURE RETENTION POLICY TO RECOVERY WINDOW OF 7 DAYS;
CONFIGURE ARCHIVELOG DELETION POLICY BACKED UP 1 TIMES TO DEVICE TYPE DISK;
```

```bash
# Thêm vào crontab: tự dọn mỗi đêm
0 1 * * * rman target / << 'EOF'
DELETE NOPROMPT OBSOLETE;
DELETE NOPROMPT ARCHIVELOG ALL BACKED UP 1 TIMES TO DEVICE TYPE DISK;
EXIT;
EOF
```

</details>

---

### Fire Drill 4 — Datafile 4 bị corrupt, cần recover không down DB

**Bối cảnh**: Giờ hành chính. `ORA-01578: block corrupted file#4 block#202`. Database OPEN, datafile 4 thuộc tablespace `USERS` (không critical). Có Image Copy mới nhất từ đêm qua.

**Nhiệm vụ**: Recover datafile 4 mà không shutdown database.

<details>
<summary>▶ Xem đáp án</summary>

```sql
-- Bước 1: Offline datafile 4 (không cần shutdown)
ALTER DATABASE DATAFILE 4 OFFLINE;
```

```rman
rman target /

-- Bước 2a: Nếu dùng Image Copy (RTO nhanh nhất)
SWITCH DATAFILE 4 TO COPY;     -- Trỏ về Image Copy
RECOVER DATAFILE 4;             -- Apply archive logs

-- Bước 2b: Nếu dùng Backup Set (không có Image Copy)
RESTORE DATAFILE 4;
RECOVER DATAFILE 4;
```

```sql
-- Bước 3: Đưa datafile online lại
ALTER DATABASE DATAFILE 4 ONLINE;
```

```rman
-- Bước 4 (nếu có nhiều block corrupt): Dùng DRA
LIST FAILURE;
ADVISE FAILURE;
REPAIR FAILURE;
```

**Hoặc Block Media Recovery nếu chỉ vài block**:
```rman
RECOVER DATAFILE 4 BLOCK 202;
```

</details>

---

### Fire Drill 5 — Clone DB production sang môi trường dev qua mạng

**Bối cảnh**: Cần tạo bản dev database `ORADB_DEV` từ production `ORADB` ngay lập tức. Không có thời gian copy backup ra disk. Destination server đã cài Oracle, listener đang chạy. DB_CREATE_FILE_DEST trên dev = `/oracle/dev`.

**Nhiệm vụ**: Active Database Duplication.

<details>
<summary>▶ Xem đáp án</summary>

```bash
# Bước 1: Trên destination server — tạo PFILE/SPFILE tối thiểu cho auxiliary
# Thêm vào /oracle/product/12c/db/dbs/initORADB_DEV.ora:
echo "DB_NAME=ORADB_DEV" >> /oracle/product/12c/db/dbs/initORADB_DEV.ora
```

```bash
# Bước 2: Startup auxiliary instance NOMOUNT
sqlplus / as sysdba << 'EOF'
STARTUP NOMOUNT PFILE='/oracle/product/12c/db/dbs/initORADB_DEV.ora';
EXIT;
EOF
```

```bash
# Bước 3: Kết nối RMAN với cả 2 DB
rman TARGET sys/oracle@oradb AUXILIARY sys/oracle@oradb_dev
```

```rman
-- Bước 4: Duplicate
DUPLICATE DATABASE TO oradb_dev
  FROM ACTIVE DATABASE
  PASSWORD FILE
  SPFILE
    SET DB_NAME 'ORADB_DEV'
    SET DB_UNIQUE_NAME 'ORADB_DEV'
    SET DB_CREATE_FILE_DEST '/oracle/dev'
    SET LOG_FILE_NAME_CONVERT '/oradata/oradb/', '/oracle/dev/'
    SET DB_FILE_NAME_CONVERT '/oradata/oradb/', '/oracle/dev/';
```

**Sau khi hoàn tất**: RMAN tự đổi DBID của ORADB_DEV → độc lập hoàn toàn với production.

</details>

---

## Phần 5 – Bảng lệnh tra cứu nhanh

### RMAN Commands — Nhóm Backup

| Lệnh | Mục đích |
|------|---------|
| `BACKUP DATABASE PLUS ARCHIVELOG` | Full backup chuẩn production |
| `BACKUP INCREMENTAL LEVEL 0 DATABASE` | Full base cho incremental strategy |
| `BACKUP INCREMENTAL LEVEL 1 DATABASE` | Differential incremental |
| `BACKUP INCREMENTAL LEVEL 1 CUMULATIVE DATABASE` | Cumulative incremental |
| `BACKUP AS COMPRESSED BACKUPSET DATABASE` | Full backup với nén |
| `BACKUP COPIES 2 DATABASE FORMAT '/d1/%U','/d2/%U'` | Duplex backup 2 vị trí |
| `BACKUP SECTION SIZE 500M DATAFILE 4` | Multisection backup file lớn |
| `BACKUP DATABASE TAG 'YEAREND' KEEP FOREVER` | Archival backup vĩnh viễn |
| `BACKUP NOT BACKED UP SINCE TIME 'SYSDATE-1' DATABASE` | Resume backup bị đứt |
| `BACKUP TABLESPACE users TAG 'MONTHLY'` | Backup partial tablespace |

### RMAN Commands — Nhóm Reporting

| Lệnh | Mục đích |
|------|---------|
| `LIST BACKUP SUMMARY` | Xem kho backup ngắn gọn |
| `LIST BACKUP OF DATABASE` | Chi tiết backup theo database |
| `LIST COPY OF DATABASE` | Xem Image Copies |
| `REPORT NEED BACKUP` | File chưa đủ backup theo retention |
| `REPORT OBSOLETE` | File hết hạn retention |
| `REPORT UNRECOVERABLE` | File có NOLOGGING operations |
| `CROSSCHECK BACKUP` | Xác nhận file vật lý còn tồn tại |
| `CROSSCHECK ARCHIVELOG ALL` | Kiểm tra archive logs vật lý |
| `DELETE EXPIRED BACKUP` | Xóa record file đã mất vật lý |
| `DELETE NOPROMPT OBSOLETE` | Xóa backup hết hạn (không hỏi) |

### RMAN Commands — Nhóm Recovery

| Lệnh | Mục đích |
|------|---------|
| `RESTORE DATABASE` | Restore toàn bộ database |
| `RECOVER DATABASE` | Apply archivelogs (complete recovery) |
| `RESTORE DATAFILE 4` | Restore 1 datafile |
| `RECOVER DATAFILE 4` | Recover 1 datafile |
| `RESTORE CONTROLFILE FROM AUTOBACKUP` | Khôi phục control file |
| `RESTORE TABLESPACE users` | Restore 1 tablespace |
| `RECOVER TABLESPACE hr UNTIL TIME '...'` | TSPITR |
| `RECOVER TABLE HR.EMP UNTIL TIME '...'` | Table recovery |
| `RECOVER DATAFILE 4 BLOCK 202` | Block Media Recovery |
| `SWITCH DATAFILE 4 TO COPY` | Dùng ngay Image Copy |
| `LIST FAILURE` | DRA: liệt kê lỗi |
| `ADVISE FAILURE` | DRA: tư vấn giải pháp |
| `REPAIR FAILURE` | DRA: tự sửa lỗi |

### RMAN Configure — Settings

| Lệnh | Mục đích |
|------|---------|
| `CONFIGURE CONTROLFILE AUTOBACKUP ON` | Tự backup control file |
| `CONFIGURE RETENTION POLICY TO RECOVERY WINDOW OF 7 DAYS` | Retention 7 ngày |
| `CONFIGURE RETENTION POLICY TO REDUNDANCY 3` | Giữ 3 bản backup |
| `CONFIGURE CHANNEL DEVICE TYPE DISK FORMAT '/u02/bkp/%U.bkp'` | Default format |
| `CONFIGURE DEVICE TYPE DISK PARALLELISM 2` | 2 channel tự động |
| `CONFIGURE COMPRESSION ALGORITHM 'MEDIUM'` | Compression level |
| `CONFIGURE ARCHIVELOG DELETION POLICY BACKED UP 1 TIMES TO DEVICE TYPE DISK` | Auto-delete archivelogs |
| `SHOW ALL` | Xem tất cả settings hiện tại |
| `CONFIGURE ... CLEAR` | Reset về default |

### SQL Commands — Monitoring & Admin

| Lệnh | Mục đích |
|------|---------|
| `SELECT * FROM V$RECOVERY_AREA_USAGE` | FRA usage theo loại file |
| `SELECT SID, SOFAR, TOTALWORK, ROUND(SOFAR/TOTALWORK*100,2) FROM V$SESSION_LONGOPS WHERE OPNAME LIKE 'RMAN%'` | Tiến độ RMAN realtime |
| `SELECT * FROM V$RMAN_OUTPUT` | Output RMAN job gần nhất |
| `SELECT * FROM RC_RMAN_BACKUP_JOB_DETAILS` | Lịch sử backup (từ Catalog) |
| `ALTER DATABASE ENABLE BLOCK CHANGE TRACKING` | Bật BCT |
| `ALTER SYSTEM SET DB_RECOVERY_FILE_DEST_SIZE = 50G SCOPE=BOTH` | Tăng FRA size |
| `SELECT PLATFORM_NAME, ENDIAN_FORMAT FROM V$TRANSPORTABLE_PLATFORM` | Kiểm tra endian |

---

## Phần 6 – Sơ đồ quyết định Recovery

```
SỰ CỐ XẢY RA
      │
      ▼
Xác định loại failure
      │
      ├──► Instance Crash (power off, SHUTDOWN ABORT)
      │         └─► STARTUP → Oracle tự Instance Recovery (SMON)
      │
      ├──► Media Failure (mất datafile, corruption)
      │         │
      │         ├── Có Image Copy? ──► SWITCH DATAFILE TO COPY → RECOVER (không cần RESTORE)
      │         └── Có Backup Set? ──► RESTORE → RECOVER → ONLINE
      │
      ├──► User Error (DROP TABLE, DELETE nhầm)
      │         │
      │         ├── 1 table? ──────► RECOVER TABLE ... UNTIL TIME (Table Recovery)
      │         ├── 1 tablespace? ──► RECOVER TABLESPACE ... UNTIL TIME (TSPITR)
      │         └── Toàn DB? ───────► PITR → OPEN RESETLOGS
      │
      ├──► Block Corruption (ORA-01578)
      │         │
      │         ├── 1-vài block? ──► RECOVER DATAFILE X BLOCK Y (BMR)
      │         ├── Nhiều block? ──► LIST FAILURE → ADVISE FAILURE → REPAIR FAILURE (DRA)
      │         └── Validate? ─────► VALIDATE DATABASE CHECK LOGICAL
      │
      └──► Control File mất
                └─► SET DBID → STARTUP NOMOUNT → RESTORE CONTROLFILE FROM AUTOBACKUP
                    → MOUNT → RESTORE DATABASE → RECOVER DATABASE → OPEN RESETLOGS
```

### Ma trận RTO vs Giải pháp

| Yêu cầu | Giải pháp | Ghi chú |
|---------|-----------|---------|
| RTO < 10 phút | **Image Copy + SWITCH** | Không cần RESTORE |
| RTO 1–4 giờ | **Backup Set + RESTORE** | Tốc độ phụ thuộc I/O |
| RPO = 0 | **Data Guard SYNC** | Ngoài phạm vi RMAN thuần túy |
| Recover 1 table | **Table Recovery** | Cần Auxiliary destination |
| Recover 1 tablespace về quá khứ | **TSPITR** | Cần Auxiliary destination |
| Compliance 5-7 năm | **KEEP FOREVER + Catalog** | Bắt buộc có Recovery Catalog |
| Off-site backup | **Password Encryption + Tape** | Mã hóa bắt buộc trước khi rời site |

---

## Đáp án Mock Exam

| Câu | Đáp án | Giải thích ngắn |
|-----|--------|-----------------|
| 1 | **D** | ORA-19809 khi 100% — nhưng DB bắt đầu cảnh báo từ 85%, critical 97% |
| 2 | **B** | DROP TABLE là user error → cần PITR (Incomplete Recovery) |
| 3 | **C** | CROSSCHECK kiểm tra sự tồn tại vật lý của file |
| 4 | **B** | Không có CUMULATIVE → mặc định là Differential |
| 5 | **C** | `SWITCH DATAFILE N TO COPY` — không cần RESTORE |
| 6 | **B** | `ALTER DATABASE ENABLE BLOCK CHANGE TRACKING` (SQL command, không phải RMAN) |
| 7 | **B** | `DURATION 04:00 PARTIAL MINIMIZE TIME` — chạy tối đa 4 giờ |
| 8 | **C** | rcowner hoặc tên DBA tự đặt — phải có quyền RECOVERY_CATALOG_OWNER |
| 9 | **C** | Encryption chỉ áp dụng cho Backup Set |
| 10 | **C** | LIST → ADVISE → REPAIR → CHANGE FAILURE CLOSED |
| 11 | **B** | Phải CONVERT trước (khác endian) |
| 12 | **B** | Giữ 3 bản gần nhất, cái cũ hơn → OBSOLETE |
| 13 | **B** | `PLUS ARCHIVELOG`: archivelogs hiện có → backup datafiles → archivelogs mới → backup |
| 14 | **B** | Chỉ xóa archive log khi đã backup ít nhất 1 lần |
| 15 | **B** | `ALTER DATABASE CLEAR LOGFILE GROUP n` (không cần recovery) |
| 16 | **B** | `KEEP FOREVER` — cần kết hợp với RESTORE POINT |
| 17 | **B** | REMAP = đổi tên đích, tránh ghi đè bảng gốc |
| 18 | **B** | COPIES 2 = 2 bản giống hệt, 2 vị trí khác nhau (Duplex) |
| 19 | **B** | Active Duplication qua Oracle Net, không cần file backup |
| 20 | **B** | V$SESSION_LONGOPS theo dõi tiến độ operation dài |
| 21 | **B** | PARALLELISM 2 = tự allocate 2 channel khi không dùng RUN{} |
| 22 | **B** | RESETLOGS đặt lại SCN sequence, tránh conflict archivelogs |
| 23 | **B** | CHECK LOGICAL bổ sung logical corruption detection |
| 24 | **D** | Backup từ ROOT = ROOT + tất cả PDB + archive logs |
| 25 | **B** | Autobackup sau mỗi BACKUP command hoặc structural change |
| 26 | **B** | Table Recovery = 1 table; TSPITR = nguyên tablespace |
| 27 | **B** | CATALOG START WITH = đăng ký file backup ngoài vào catalog |
| 28 | **C** | RAC: Snapshot CF phải trên Shared Storage (ASM) |
| 29 | **B** | MULTISECTION hữu ích khi datafile lớn, muốn backup song song |
| 30 | **B** | EXPIRED record không dùng được → DELETE EXPIRED BACKUP |

---

## Checklist trước khi apply production

Trước khi chạy bất kỳ lệnh recovery nào trên production, kiểm tra:

- [ ] Xác định chính xác loại failure (instance / media / user error / block)
- [ ] Có backup hợp lệ không? (`LIST BACKUP SUMMARY`, `CROSSCHECK BACKUP`)
- [ ] ARCHIVELOG mode đang bật? (`ARCHIVE LOG LIST`)
- [ ] FRA còn dung lượng không? (`V$RECOVERY_AREA_USAGE`)
- [ ] Cần Complete hay Incomplete Recovery?
- [ ] Nếu PITR → thông báo users về khoảng thời gian bị mất data
- [ ] Auxiliary destination có đủ space không? (cho TSPITR / Table Recovery)
- [ ] Sau recovery → backup ngay lập tức

---

*Tài liệu ôn tập toàn khóa — Oracle RMAN 17 Modules (Ahmed Baraka, Packt)*
*Tạo: 2026-04-30 | Lab: 192.168.1.8 | SID: ORADB*


---

!!! info "Nguồn gốc"
    `dba_ai/oracle_rman/reviews_all/20260430_OntapTongHop_M01M17.md`
